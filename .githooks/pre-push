#!/usr/bin/env bash
set -euo pipefail

PASS='\033[0;32m✓\033[0m'
FAIL='\033[0;31m✗\033[0m'

echo "  Running tests before push..."
if ! zig build test 2>&1; then
    echo -e "  $FAIL Tests failed. Fix before pushing, or use: git push --no-verify"
    exit 1
fi
echo -e "  $PASS All tests passed"
